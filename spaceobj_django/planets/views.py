from django.shortcuts import render

# Create your views here.
def get_planets(request):
    """
    To be coded. This function will return a json containing 
    the details of all custom planets for the front end to 
    display.
    """


def create_planet(request):
    """
    To be coded. This function will take in planet data in
    JSON format and use a cursor function to write a SQL INSERT
    into the custom_planets.sql file.
    """